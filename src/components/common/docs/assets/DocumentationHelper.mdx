export function SbContainer(props) {
    return <div className="sb-container">{props.children}</div>
}

export function SbContainerHeader(props) {
    return <div className="sb-section-title">{props.children}</div>
}

export function SbSectionTwoColumns(props) {
    return <div className="sb-section sb-section--grid">{props.children}</div>
}

export function SbSectionItem(props) {
    return <div className="sb-section-item">{props.children}</div>
}

export function SbCallout(props) {
    return <div className={`sb-callout ${props.important ? "sb-callout--important" : ""}`}>{props.children}</div>
}

export function SbCalloutText(props) {
    return <div className="sb-callout-text">{props.children}</div>
}

export function SbCalloutImg(props) {
    return <div className="sb-callout-img"><SbImg src={props.src} alt={props.alt}></SbImg></div>
}

export function SbImg(props) {
    return <img src={props.src} alt={props.alt} />
}

export function SbImgIcon (props) {
    return <img className="icon" src={props.src} alt={props.alt} />
}

export function SbSectionLinks(props) {
    return <div className="sb-section sb-section--links">{props.children}</div>
}

export function SbExternalLink(props) {
    return <a href={props.href} target="_blank" rel="noopener noreferrer">{props.children}<SbRightArrow /></a>
}


export const SbRightArrow = () => (
    <svg
        viewBox="0 0 14 14"
        width="8px"
        height="14px"
        style={{
            marginLeft: "4px",
            display: "inline-block",
            shapeRendering: "inherit",
            verticalAlign: "middle",
            fill: "currentColor",
            "path fill": "currentColor",
        }}
    >
        <path d="m11.1 7.35-5.5 5.5a.5.5 0 0 1-.7-.7L10.04 7 4.9 1.85a.5.5 0 1 1 .7-.7l5.5 5.5c.2.2.2.5 0 .7Z" />
    </svg>
);


export const SbDocsStyles = () => (
    <style>
{`
    img {
        object-fit: cover;
        border-radius: 5px;
        border: 1px solid rgb(var(--surface-200) / 1);
    }

    img.icon {
        height: 32px;
        width: 32px;
        margin-right: 8px;
        object-fit: contain;
        border-radius: 0px;
        border: 0px;
    }

    a:has(img) {
        transition: filter 0.2s ease-in-out;
    }
    a:has(img):hover {
        filter: brightness(0.9);
    }



    .sb-container {
        margin-bottom: 48px;
    }

    .sb-section-title {
        margin-bottom: 32px;
    }

    .sb-section {
        width: 100%;
        display: flex;
        flex-direction: row;
        gap: 20px;
    }

    .sb-section--grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        grid-gap: 32px 20px;
    }

    .sb-section--links {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
    }

    .sb-section a:not(h1 a, h2 a, h3 a) {
        font-size: 14px;
    }

    .sb-section-item {
        flex: 1;
        display: flex;
        flex-direction: column;
    }

    .sb-section-item h1,
    .sb-section-item h2,
    .sb-section-item h3,
    .sb-section-item h4 {
        padding-top: 20px !important;
        padding-bottom: 5px !important;
        margin: 0 !important;
    }

    .sb-section-item p,
    .sb-section--links p {
        margin: 0;
        padding-bottom: 10px;
    }

    .sb-section-item ul,
    .sb-section--links ul {
        margin: 0;
        padding-bottom: 10px;
    }

    .sb-section-item li,
    .sb-section--links li {
        margin: 0;
    }

    .sb-section-item .docblock-source {
        margin-top: 0px;
    }



    .sb-callout {
        width: 100%;
        display: flex;
        flex-direction: row;
        gap: 20px;
        align-items: center;
        justify-content: space-between;
        background: rgb(var(--surface-50) / 1);
        border: 1px solid rgb(var(--surface-200) / 1);
        border-radius: 5px;
        min-height: 180px;
        margin-bottom: 48px;
        overflow: hidden;
        position: relative;
    }

    .sb-callout--important {
        background: rgb(var(--primary-400) / .1);
        border: 1px solid rgb(var(--primary-400) / 1);
    }

    .sb-callout-text {
        padding: 48px 0px 38px 48px;
        flex: 0 0 33%;
    }

    .sb-callout-text:only-child {
        padding: 48px 48px 38px 48px;
        flex: 0 0 100%;
    }

    .sb-callout-text > * {
        margin: 0;
        padding-bottom: 10px;
    }

    .sb-callout-text h3,
    .sb-callout-text h4 {
        padding-top: 0px;
    }

    .sb-callout-img {
        overflow: hidden;
        height: 100%;
        width: 60%;
        position: absolute;
        right: 0;
    }

    .sb-callout-img img {
        border-radius: 0px;
        border: 0px;
        filter: opacity(0.7);
        position: absolute;
        width: 100%;
        height: 100%;
        object-fit: cover;
        object-position: left center;
    }

    @media screen and (max-width: 768px) {
        .sb-section {
            flex-direction: column;
        }

        .sb-section--grid {
            grid-template-columns: repeat(1, 1fr);
        }

        .sb-section--links {
            grid-template-columns: repeat(2, 1fr);
        }

        .sb-callout {
            flex-direction: column;
            min-height: 280px;
            align-items: flex-start;
            gap: 0px;
        }

        .sb-callout-text {
            padding: 24px 24px 14px 24px;
        }

        .sb-callout-img {
            width: 100%;
            position: relative;
            height: 200px;
        }
    }
`}
</style>)
